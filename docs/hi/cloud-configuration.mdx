---
title: क्लाउड कॉन्फ़िगरेशन
description: 'राउटेड DDoS मिटिगेशन के लिए क्लाउड कंसोल में टनल, ASN और रूट कॉन्फ़िगर करें।'
sidebar:
  order: 4
i18n:
  sourceHash: 606c77582a14
  translator: machine
---

## XC (UI)

[L3/L4 राउटेड DDoS मिटिगेशन](https://docs.cloud.f5.com/docs-v2/ddos-and-transit-services/how-tos/network-firewall/l3l4-ddos-mitigation) गाइड के आधार पर **क्लाउड** साइड को कॉन्फ़िगर करने के लिए वेब इंटरफ़ेस का उपयोग करें।

### DDoS वर्कस्पेस सक्षम करें

टनल और BGP कॉन्फ़िगर करने से पहले:

1. क्लाउड टीम से संपर्क करें (`sales@example.com`
   या अपनी अकाउंट टीम के माध्यम से) ताकि:
    - अपने टेनेंट के लिए **राउटेड DDoS मिटिगेशन** वर्कस्पेस सक्षम करवाएं।
    - निम्नलिखित प्रदान करें:
        - आपके **पब्लिक IP नेटब्लॉक** (IPv4 के लिए /24 या छोटे, IPv6 के लिए /48 या
          छोटे) और स्वामित्व का प्रमाण (या LOA)।
        - आपका **ASN** (ARIN या समकक्ष रजिस्ट्री द्वारा जारी होना चाहिए)।
        - **IRR** पंजीकरण (RIPE, ARIN, APNIC, आदि) और RPKI
          रिपॉज़िटरी में **ROA**।
        - वांछित **क्लीन-ट्रैफ़िक रिटर्न मेथड** (GRE टनल, लेयर 2,
          Equinix के माध्यम से प्राइवेट पीयरिंग, आदि)।
        - डेटा सेंटर लोकेशन और सुरक्षित करने योग्य राउटर।
        - रूट एनाउंसमेंट के लिए आवश्यक **AS-Path प्रीपेंड्स**।
        - कोई भी **BGP कम्युनिटीज़**, रूट प्रेफरेंस, या **AS-SET**

1. SOC प्रोविज़न करेगा:
    - टनल
    - ASN
    - संरक्षित प्रीफ़िक्स
    - रूट एडवर्टाइज़मेंट विकल्प
    - आवश्यकतानुसार फ़ास्ट ACL / फ़ायरवॉल पॉलिसी

### टनल

1. [क्लाउड कंसोल](https://docs.cloud.f5.com/docs-v2/ddos-and-transit-services/how-tos/network-firewall/l3l4-ddos-mitigation) में लॉग इन करें और
   सर्विस सेलेक्टर से **Routed DDoS** चुनें।
1. **Manage > Tunnels > Add Tunnel** पर जाएं, और कॉन्फ़िगर करें:
    - **Location Name** और **Availability Zone** (डिफ़ॉल्ट रूप से Zone 1)।
    - **Bandwidth Max in MB**।
    - **Tunnel Type**:
        - IPv4 आउटर टनल के लिए **GRE Over IPv4**।
        - IPv6 आउटर टनल के लिए **GRE Over IPv6**।
        - IPv4-in-IPv4 एनकैप्सुलेशन के लिए **IP Over IP**।
        - IPv6-in-IPv6 एनकैप्सुलेशन के लिए **IPv6 Over IPv6**।
    - **Customer Endpoint IP**: BIG-IP का एक्सटर्नल एड्रेस (आउटर सेल्फ
      IP, जब टनल पब्लिक इंटरनेट से होकर गुज़रती हैं तो यह पब्लिकली
      राउटेबल होना चाहिए)।
    - वैकल्पिक: IPv4/IPv6 इंटरकनेक्ट, फ़्रेगमेंटेशन, कीपअलाइव (सभी
      डिफ़ॉल्ट रूप से अक्षम)।
1. **Tunnel BGP Information** के अंतर्गत:
    - एक **ASN ऑब्जेक्ट** (आपका ASN) चुनें।
    - **Customer Peer Secret Override** सेट करें: Use Default Secret
      (डिफ़ॉल्ट), BGP Password Override (ब्लाइंडफ़ोल्डेड या क्लियर टेक्स्ट), या
      No Secret।
    - यदि डिफ़ॉल्ट से भिन्न हो तो **Holddown Timer** मान सेकंड में
      सेट करें।

SOC आपके लिए ये टनल ऑब्जेक्ट पहले से बना सकता है; आपको बस
BIG-IP पर **एंडपॉइंट IP** और BGP सेटिंग्स मिलान करनी होंगी।

### ASN और रूट

- **ASN**: -> **Manage > ASNs > Add ASN**।
    - अपना ASN दर्ज करें और सुनिश्चित करें कि BGP सक्षम है।

- **प्रीफ़िक्स**: -> **Manage > Prefixes > Add Prefix**।
    - प्रत्येक IP प्रीफ़िक्स दर्ज करें और इसे अपने ASN से संबद्ध करें।

- **रूट एडवर्टाइज़मेंट** -> **Manage > Route Advertisement > Add Route Advertisement**।
    - प्रीफ़िक्स दर्ज करें, **Active** या **Not Advertised** चुनें, और
      वैकल्पिक समाप्ति तिथि।

ये ऑब्जेक्ट नियंत्रित करते हैं कि सेवा सक्रिय होने पर ग्लोबल
नेटवर्क के माध्यम से कौन से प्रीफ़िक्स एनाउंस किए जाते हैं।

### नेटवर्क प्रतिबंध

**Firewall Rules**, **Deny List Rules**, और **Fast ACLs for Internet
VIPs** आपको इन कार्यों की अनुमति देते हैं:

- विशिष्ट ट्रैफ़िक को ब्लॉक या अनुमति दें।
- दुर्भावनापूर्ण स्रोतों को रेट-लिमिट करें।
- शुद्ध वॉल्यूमेट्रिक स्क्रबिंग से परे अतिरिक्त DDoS सुरक्षा लागू करें।
